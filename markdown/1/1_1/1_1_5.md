# 1.1.5 命令行环境

## 视频笔记

- 任务控制
  - UNIX 提供的信号机制 `signal`
    - `man signal` 或 `kill -t` 查阅关于信号的信息
    - 发送信号的方式：
      - `Ctrl + C` --> `SIGINT`
      - `Ctrl + \` --> `SIGQUIT`
      - `Ctrl + Z` --> `SIGTSTP`
      - `kill` --> 可发生任意信号
  - 一些指令
    - `nohup`
    - `jobs`
    - `bg`
    - `fg`
    - `kill`

- 终端多路复用
  - tmux
    - 会话 (session)
      - `tmux`: `a` `new` `ls` `-t`
      - `<C-b> d`
      - `Ctrl + d`
    - 窗口 (window)
      - `<C-b> c`
      - `<C-b> p`
      - `<C-b> n`
      - `<C-b> 1`
      - `<C-b> ,`
    - 窗格 (panes)
      - `<C-b> "`
      - `<C-b> %`
      - `<C-b> 方向键`
      - `<C-b> Space`
      - `<C-b> z`
      - `<C-b> [`

- 别名
  - `alias`
  - 把别名保存到配置文件中

- 配置文件
  - 点文件
  - 统一管理配置文件

- 远程设备
  - ssh
    - ssh连接和执行命令
    - ssh密钥
    - 一些命令
      - `ssh-copy-id`
      - `ssh-keygen`
      - `scp`
      - `rsync`
    - ssh config
    - tmux的作用

## 课后练习

- 任务控制
  - [1] 使用 pgrep 来查找 pid 并使用 pkill 结束进程而不需要手动输入pid。

  ```bash
  sleep 10000
  Ctrl-Z
  bg
  pgrep sleep
  pkill sleep
  ```

  - [2] 果您希望某个进程结束后再开始另外一个进程， 应该如何实现呢？

  ```bash
  # 错误方法
  # 下面这条命令会先输出ls的内容，等sleep执行后再输出wait的内容
  sleep 60 | wait | ls

  # 有效做法
  sleep 60 | wait && ls
  ```

  ```bash
  pidwait() {
    while kill -0 "$1" 1> /dev/null 2>&1
    do
      sleep 1
    done
    ls
  }
  ```

- 终端多路复用

  - [1] 请完成这个 tmux 教程 参考这些步骤来学习如何自定义 tmux。
    - 自定义配置的 `.tmux.conf` 文件已经发布到我的GitHub上的dotfiles仓库中。

- 别名

  - [1] 创建一个 dc 别名，它的功能是当我们错误的将 cd 输入为 dc 时也能正确执行。

  ```bash
  alias dc="cd"
  ```

  - [2] 获取您最常用的十条命令，尝试为它们创建别名。

  ```bash
  # alias
  alias dc="cd"

  alias l="ls"
  alias ll="ls -l"
  alias la="ls -al"

  alias cdh="cd ~"
  ```

- 配置文件
  - [我的dotfiles仓库](`https://github.com/AbsurdMirror/dotfiles`)

- 远端设备
  - *未做*

## 返回首页

  [首页](../../../README.md)
